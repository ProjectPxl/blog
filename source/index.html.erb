<ul>
  <% if blog.articles == []%>
    Paul hasn't written anything yet! Come back later...
  <%else%>
    <% blog.articles.group_by {|a| a.date.year }.each do |year, articles| %>
      <% articles.each_with_index do |article| %>
        <li class="portfolio__item">
          <a href="<%= article.url %>">
            <img class="portfolio__item--thumb" src="images/<%=article.data.image%>" alt="<%= article.title %>">
            <h3 class="portfolio__item--title"><%= article.title %></h3>
          </a>
        </li>
      <% end %>
    <% end %>
  <% end %>
</ul>

